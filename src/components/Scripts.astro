<script>
    // const { signIn, signOut } = await import("auth-astro/client");

    // async function login() {
    //     await signIn("github");
    // }

    // async function logout() {
    //     await signOut();
    // }

    // async function toggleLike(button: HTMLButtonElement) {
    //     const slug = button.getAttribute("data-like")!;
    //     const count = button.querySelector("[data-like-count]")!;
    //     if (button.classList.contains("is-active")) {
    //         button.classList.remove("is-active");
    //         count.textContent =
    //             String(Number(count.textContent ?? 0) - 1) || "";
    //         await fetch(`/api/blogs/${slug}/likes`, {
    //             method: "DELETE",
    //         });
    //     } else {
    //         button.classList.add("is-active");
    //         count.textContent = String(Number(count.textContent || 0) + 1);
    //         await fetch(`/api/blogs/${slug}/likes`, {
    //             method: "POST",
    //         });
    //     }
    // }

    // document.addEventListener(
    //     "click",
    //     function (event) {
    //         if (event.target instanceof Element) {
    //             // if (event.target.closest("[data-login]")) {
    //             //     login();
    //             //     return;
    //             // }
    //             // if (event.target.closest("[data-logout]")) {
    //             //     logout();
    //             //     return;
    //             // }
    //             // if (event.target.closest("[data-toggle-theme]")) {
    //             //     toggleTheme();
    //             //     return;
    //             // }
    //             // if (event.target.closest("[data-like]")) {
    //             //     toggleLike(
    //             //         event.target.closest("[data-like]") as HTMLButtonElement
    //             //     );
    //             //     return;
    //             // }
    //         }
    //     },
    //     true
    // );
</script>
